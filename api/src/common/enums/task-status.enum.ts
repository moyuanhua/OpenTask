export enum TaskStatus {
  PENDING = 'pending',
  PLANNING = 'planning',
  IN_PROGRESS = 'in_progress',
  BLOCKED = 'blocked',
  WAITING_APPROVAL = 'waiting_approval',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
